# Working directory outputs (created at runtime on the CSV)
Logs/
Backups/
Temp/
Configs/

# User-specific config (actual values — never commit)
config/variables.yml

# Log and report files
*.log
*.csv

# Exported VM config JSON files
*_config.json
*_arc_info.json

# PowerShell session artifacts
*.pssproj.user

# VS Code
.vscode/

# Windows
Thumbs.db
desktop.ini
